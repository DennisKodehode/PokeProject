import heroSlide from "../assets/heroSlide.png"

export const Hero = () => {
    // placeholder for now
    return (
        <div className="hero-image-wrapper">
            <img className="hero-image" src={heroSlide} alt="" />
        </div>
        
    )
}